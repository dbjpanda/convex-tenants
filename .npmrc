; CI/Vercel: tolerate unresolved peer-dep ranges introduced by mixed
; eslint v10 ecosystem packages. The publish workflow already passes
; --legacy-peer-deps explicitly; this makes other npm install runs
; (Vercel preview deploys, contributor checkouts, etc.) match.
legacy-peer-deps=true
